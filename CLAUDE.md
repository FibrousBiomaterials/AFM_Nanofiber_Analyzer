# Agent Instructions

@AGENTS.md

`AGENTS.md` is the canonical instruction file for all AI coding agents in
this repository; the `@AGENTS.md` line above imports it for Claude Code.
Do not add rules to this file — edit `AGENTS.md` instead so every agent
(Claude Code, Codex, and others) sees the same rules.
